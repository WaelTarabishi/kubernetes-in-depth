apiVersion: v1
kind: Pod
metadata:
  name: configmap-env-pod
  namespace: learning
  labels:
    app: configmap-env-pod
spec:
  containers:
    - name: busybox
      image: busybox:1.36
      command: ["sh", "-c", "sleep 3600"]
      envFrom:
        - configMapRef:
            name: app-config
